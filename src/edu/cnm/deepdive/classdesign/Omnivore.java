package edu.cnm.deepdive.classdesign;

public interface Omnivore extends Herbivore, Carnivore{

  // void eatPlants();
  // void eatMeat();   // so if we use extend those two interfaces, we don't need to write these two methods

}
